/* Generated by scripts/compile-drama-video-director-skill.mjs. Do not edit manually. */
export type DramaDirectorSurface = "package" | "static-frame" | "video" | "external-codex";

export const DRAMA_VIDEO_DIRECTOR_SKILL = {
    id: "drama-video-director",
    name: "短剧视频导演",
    description: "用于短剧制作包、分镜、片段首尾帧和视频提示词的导演级审阅与优化。用户提到分镜、镜头设计、片段帧、视频提示词、运镜、光影、景别、连续性或制作包时，显式选择本 Skill 以执行导演前置审计、静态帧优化和视频提示词优化；不自动改写普通对话。",
    sourcePath: "SKILL.md",
    sourceVersion: "1.1.0",
    sourceContentHash: "ec962c3664300abace1a83122a1e5ab9f8e3de36d138cc74dba93b0538c29a61",
    enabled: true,
    workspaces: ["video", "drama"],
    action: "generate",
    requiresReference: false,
    keywords: ["分镜", "镜头设计", "视频提示词", "片段帧", "首帧", "尾帧", "运镜", "光影", "连续性"],
    instructions:
        "# 短剧视频导演层\n\n这是一个 prompt-authoring-only 的导演方法层。它只把导演判断写入现有制作包字段、静态帧字段和视频公开合同，不创建第二套提示词，不提交供应商任务，也不覆盖项目资产、供应商协议和已验收尾帧事实。\n\n## 共同前置审计\n\n任何镜头先回答以下问题，再写公开提示词：\n\n1. 此刻主体想要什么，阻力来自哪里，镜头结束时发生了什么可见变化。\n2. 谁在什么位置、以什么姿势、借助什么支撑、对谁或什么做什么。\n3. 主体的视线、屏幕方向和 180 度轴线如何保持；多人必须有同一参照系。\n4. 当前镜头的唯一戏剧职责是什么：情绪变化、动作推进、信息揭示或压力增加。\n5. 哪个具体环境压力、身体微动作和声音/视觉锚点让镜头不只是空泛氛围。\n\n原文事实、资产事实、合理推断和导演建议必须分开。没有原文或资产依据时，不得增加人物、道具、关系、地点或剧情事件。\n\n## 场景 NPC 策略\n\n- 场景资产的 `backgroundNpcPolicy` 是背景群像规则，`auto` 由场景类型、空间容量、景别和剧情功能判断，`required` 必须在关键帧和视频中安排合理数量、密度、位置与行为结果，`forbidden` 则禁止背景 NPC。\n- NPC 只作为不具名背景群像，不进入主角色 `characterCodes`、角色锚点或独立角色资产；只有具有明确剧情身份的 NPC 才升级为正式角色资产。\n- 场景全景基准图始终保持高清、无人、无文字的单视角建立图；NPC 只写入实际镜头的关键帧、视频时间段和可选构图参考。没有配置策略时按 `auto` 判断，不能把“没有配置”误读成“禁止人物”。\n- `auto` 场景需要 NPC 时，必须明确写数量范围、前中后景分布、群体姿态/行为和不抢主角注意力的景深关系；不需要时明确使用空位、空通道或画外人声等事实依据。不得为所有场景套用固定人数。\n\n## 摄影与画面\n\n- 景别、焦段、机位、构图和主运镜必须共同服务一个可见的信息或动作变化。\n- 每镜只保留一个主运镜；按不同运动类型判断冲突，不按同一运镜在构图、摄影字段和公开 Prompt 中的重复出现次数判断。不要把推、拉、摇、移、旋转等互不相容的运动堆在一起。\n- 光影必须能回答光从哪里来、照到谁、如何衰减、被什么遮挡，以及材质如何反应。\n- 纵深至少由前景框景或遮挡、中景主体、背景环境中的真实元素构成；不要用“有层次”代替具体空间。\n- 景深、焦外、雾气和颗粒只用于承载叙事重点，不得遮盖需要识别的脸、手、道具或连续性状态。\n- 背景不是装饰：需要识别的门窗、墙面、桌面、通道和陈设必须保持清晰可辨；禁止用大光圈人像虚化或过重雾化掩盖场景结构。\n- “电影感、氛围感、高级感、震撼”不能单独作为视觉设计，必须翻译成光色、材质、构图、运动或可见状态。\n\n## 表演与节奏\n\n- 把抽象情绪翻译为眉眼、视线、呼吸、嘴角、重心、手部、身体和道具的可观察变化。\n- `performancePlan.emotionalObjective`、`emotionalArc` 和 start/middle/end 必须在制作包生成阶段一次写全；情绪目标不能只写“自然、到位、逐步变化、结果成立”，每个阶段至少绑定一个当前镜头的具体视线、呼吸、受力、手部、身体、道具或环境结果。导入后不得再依赖笼统兜底才变成可生产数据。\n- 动作按“起点状态 → 触发/受力 → 动作过程 → 可见结果或反应”组织；每个时间段的终点必须能成为下一段的起点。\n- 多帧镜头不得让所有片段沿用同一张构图；在不破坏轴线和连续性的前提下，根据真实信息安排景别、机位/构图、站位、视线、手部、重心或道具结果的可见变化，不能只在文字中换同义词。\n- 对白先核算自然语速和停顿，再决定镜头边界；重大冲击前保留必要停顿，高潮后给出结果或反应。\n- 非人物镜头、道具特写和环境切镜必须有信息、压力或转场职责，不按固定比例机械添加。\n\n## 自适应关键帧与镜头事件\n\n- Agent 模式默认在 2–9 帧内选择数量：稳定进入/结果用 2 帧，一个主动作加反应用 3–4 帧，多人反应、空间揭示或明显摄影变化用 5–6 帧，复杂调度或多次信息揭示才用 7–9 帧。固定 4/5 帧只在用户主动选择时执行。\n- 先列镜头事件节点，再决定帧数：动作、表情/视线、道具状态、对手或 NPC 反应、空间揭示、景别/机位/构图切换。视线转移、信息揭示、人物反应、空间关系变化或明确切镜必须有对应新关键帧。\n- 每帧必须写相对上一帧的可见变化。相同机位可以连续使用，但人物表情、姿态、重心、视线、手部、道具受力或环境结果至少有一项发生可辨识变化；画面状态和摄影状态同时重复时不得生成重复帧。\n- 镜头发生切换时，新关键帧必须说明切换发生的时间/事件、新机位服务的信息以及如何承接上一帧；不能用只改形容词的相似画面代替。\n\n## 静态帧适配器\n\n静态帧必须继续使用项目固定九段骨架：\n\n`静态关键帧 → 可见状态 → 可见表演状态 → 景别 → 机位与构图 → 站位与视线 → 三层空间 → 光色与风格 → 负面约束`\n\n静态帧只冻结一个已经发生的瞬间。可以写焦段、景深、光线和空间层次，但不得写运镜过程、时间段动作、剪辑、对白或声音。当前帧必须具体写出主体姿态、眉眼、嘴角、下颌、视线、呼吸、手部、身体重心、道具受力或环境结果，并与相邻帧形成可辨识差异；多帧镜头至少让一项摄影变量或可见表演变量发生真实变化，背景结构仍须清晰可辨。NPC 如由场景策略要求，必须写数量范围、前中后景位置、密度和可见行为结果，不得加入主角色名单。负面约束字段可以列出“无运镜过程、无对白、无声音指令”等禁止项，但这些词不能出现在正向静态字段。同一镜头内每帧只使用场景、角色、道具等固定资产锚点，不引用或等待同镜上一帧图片；只有跨镜头且连续性边明确要求时，镜头第一帧才使用上一镜已人工验收的实际尾帧 `previous_accepted_actual_tail`。\n\n## 视频适配器\n\n视频提示词继续使用项目固定公开字段：\n\n`素材绑定 → 动态意图 → 全局设定 → 起始可见状态 → 时间段动作 → 单一主运镜 → 环境压力与视觉母题 → 视觉风格与光色 → 声音意图 → 结束画面 → 连续性锁 → 针对性约束`\n\n每个真实时间段逐块写出起点、动作与触发、可见衔接和终点；必须有具体时间范围，不能机械逐秒切分。动作必须能被看见，至少落到身体、表情/视线、手部/道具或环境结果；NPC 如被场景策略要求，时间段必须写清群像的数量范围、位置、密度和反应，不得让 NPC 变成无依据的主角。摄影必须写明一个有动机的主运镜或固定机位及其服务的变化；镜头切换必须写出发生时间、触发事件、信息目的和承接关系，背景结构不得被虚化成无信息色块。视频 prompt 和 `framePlan.frames` 由 Agent 直接生成，应用代码只校验、保存和转发，不能根据帧计划重新拼接或补写视频 prompt。\n\n## 制作包适配器\n\n制作包镜头必须把导演判断落到现有字段：\n\n- `dramaticFunction`：唯一戏剧职责和情绪/压力变化。\n- `performancePlan`：起点、中段、终点的可见表演变化。\n- `lightingPlan`：色板、色温、光源方向、材质反应、肤色保护和前后镜衔接。\n- `continuity`：景别、机位、构图、站位、视线、动作起止、屏幕方向和轴线；同镜头关键帧独立生成，`previous_accepted_actual_tail` 只允许作为跨镜头第一帧的入口连续性依据。\n- `lens`、`cameraMotion`、`transitionIn`、`transitionOut`：摄影与转场的可执行选择。\n- `entryState`、`exitState`：本镜头可被下一镜继承的事实状态。\n- `backgroundNpcPolicy`：场景默认/必须/禁止背景 NPC，以及本镜头实际采用的数量、分布和行为；NPC 不进入角色资产引用。\n- `productionPlan.references.maxImages` 必须随镜头时长生成：15 秒及以下为 9，30 秒为 30；这只是短剧编排预算，供应商实际声明的更小能力仍由运行时能力校验决定。\n\n不得新增重复章节，不得用占位词代替当前节点事实。\n\n## 门禁分层\n\n硬门禁只处理事实和可执行性：资产引用、人物/道具/空间是否真实、当前帧是否有可见状态、入口/出口状态、实际尾帧、时间轴、参考职责和连续性依据。\n\n软警告处理电影化质量：光源动机、焦段与机位意图、纵深机制、景深使用、轴线风险、节奏重复、高潮停顿、非人物镜头职责和空泛电影术语。经验比例、固定运镜次数和题材偏好不得直接阻断生成。\n\n## 外部 Codex 调用\n\n仅在用户显式选择本 Skill，或明确要求“审阅/优化制作包、分镜、片段帧、首尾帧、视频提示词”时执行。输出应说明发现的问题、保留的事实和可直接替换的公开字段；不得把内部规则、模型理由、供应商参数、内部 ID 或思维链写入公开提示词。\n\n## 项目定制规则\n\n执行优先级固定为：本次用户补充要求 > 项目保存的 `customDirectorRules` > 当前剧本与资产事实 > 本 Skill > 系统默认。项目定制规则长期影响制作包、图片帧优化、视频提示词优化和外部 Codex 工作单，但不得覆盖真实资产、连续性验收结果、供应商能力或用户本次明确修改。",
    sections: {
        common: "任何镜头先回答以下问题，再写公开提示词：\n\n1. 此刻主体想要什么，阻力来自哪里，镜头结束时发生了什么可见变化。\n2. 谁在什么位置、以什么姿势、借助什么支撑、对谁或什么做什么。\n3. 主体的视线、屏幕方向和 180 度轴线如何保持；多人必须有同一参照系。\n4. 当前镜头的唯一戏剧职责是什么：情绪变化、动作推进、信息揭示或压力增加。\n5. 哪个具体环境压力、身体微动作和声音/视觉锚点让镜头不只是空泛氛围。\n\n原文事实、资产事实、合理推断和导演建议必须分开。没有原文或资产依据时，不得增加人物、道具、关系、地点或剧情事件。",
        cinematography:
            "- 景别、焦段、机位、构图和主运镜必须共同服务一个可见的信息或动作变化。\n- 每镜只保留一个主运镜；按不同运动类型判断冲突，不按同一运镜在构图、摄影字段和公开 Prompt 中的重复出现次数判断。不要把推、拉、摇、移、旋转等互不相容的运动堆在一起。\n- 光影必须能回答光从哪里来、照到谁、如何衰减、被什么遮挡，以及材质如何反应。\n- 纵深至少由前景框景或遮挡、中景主体、背景环境中的真实元素构成；不要用“有层次”代替具体空间。\n- 景深、焦外、雾气和颗粒只用于承载叙事重点，不得遮盖需要识别的脸、手、道具或连续性状态。\n- 背景不是装饰：需要识别的门窗、墙面、桌面、通道和陈设必须保持清晰可辨；禁止用大光圈人像虚化或过重雾化掩盖场景结构。\n- “电影感、氛围感、高级感、震撼”不能单独作为视觉设计，必须翻译成光色、材质、构图、运动或可见状态。",
        performance:
            "- 把抽象情绪翻译为眉眼、视线、呼吸、嘴角、重心、手部、身体和道具的可观察变化。\n- `performancePlan.emotionalObjective`、`emotionalArc` 和 start/middle/end 必须在制作包生成阶段一次写全；情绪目标不能只写“自然、到位、逐步变化、结果成立”，每个阶段至少绑定一个当前镜头的具体视线、呼吸、受力、手部、身体、道具或环境结果。导入后不得再依赖笼统兜底才变成可生产数据。\n- 动作按“起点状态 → 触发/受力 → 动作过程 → 可见结果或反应”组织；每个时间段的终点必须能成为下一段的起点。\n- 多帧镜头不得让所有片段沿用同一张构图；在不破坏轴线和连续性的前提下，根据真实信息安排景别、机位/构图、站位、视线、手部、重心或道具结果的可见变化，不能只在文字中换同义词。\n- 对白先核算自然语速和停顿，再决定镜头边界；重大冲击前保留必要停顿，高潮后给出结果或反应。\n- 非人物镜头、道具特写和环境切镜必须有信息、压力或转场职责，不按固定比例机械添加。",
        staticFrame:
            "静态帧必须继续使用项目固定九段骨架：\n\n`静态关键帧 → 可见状态 → 可见表演状态 → 景别 → 机位与构图 → 站位与视线 → 三层空间 → 光色与风格 → 负面约束`\n\n静态帧只冻结一个已经发生的瞬间。可以写焦段、景深、光线和空间层次，但不得写运镜过程、时间段动作、剪辑、对白或声音。当前帧必须具体写出主体姿态、眉眼、嘴角、下颌、视线、呼吸、手部、身体重心、道具受力或环境结果，并与相邻帧形成可辨识差异；多帧镜头至少让一项摄影变量或可见表演变量发生真实变化，背景结构仍须清晰可辨。NPC 如由场景策略要求，必须写数量范围、前中后景位置、密度和可见行为结果，不得加入主角色名单。负面约束字段可以列出“无运镜过程、无对白、无声音指令”等禁止项，但这些词不能出现在正向静态字段。同一镜头内每帧只使用场景、角色、道具等固定资产锚点，不引用或等待同镜上一帧图片；只有跨镜头且连续性边明确要求时，镜头第一帧才使用上一镜已人工验收的实际尾帧 `previous_accepted_actual_tail`。",
        video: "视频提示词继续使用项目固定公开字段：\n\n`素材绑定 → 动态意图 → 全局设定 → 起始可见状态 → 时间段动作 → 单一主运镜 → 环境压力与视觉母题 → 视觉风格与光色 → 声音意图 → 结束画面 → 连续性锁 → 针对性约束`\n\n每个真实时间段逐块写出起点、动作与触发、可见衔接和终点；必须有具体时间范围，不能机械逐秒切分。动作必须能被看见，至少落到身体、表情/视线、手部/道具或环境结果；NPC 如被场景策略要求，时间段必须写清群像的数量范围、位置、密度和反应，不得让 NPC 变成无依据的主角。摄影必须写明一个有动机的主运镜或固定机位及其服务的变化；镜头切换必须写出发生时间、触发事件、信息目的和承接关系，背景结构不得被虚化成无信息色块。视频 prompt 和 `framePlan.frames` 由 Agent 直接生成，应用代码只校验、保存和转发，不能根据帧计划重新拼接或补写视频 prompt。",
        package:
            "制作包镜头必须把导演判断落到现有字段：\n\n- `dramaticFunction`：唯一戏剧职责和情绪/压力变化。\n- `performancePlan`：起点、中段、终点的可见表演变化。\n- `lightingPlan`：色板、色温、光源方向、材质反应、肤色保护和前后镜衔接。\n- `continuity`：景别、机位、构图、站位、视线、动作起止、屏幕方向和轴线；同镜头关键帧独立生成，`previous_accepted_actual_tail` 只允许作为跨镜头第一帧的入口连续性依据。\n- `lens`、`cameraMotion`、`transitionIn`、`transitionOut`：摄影与转场的可执行选择。\n- `entryState`、`exitState`：本镜头可被下一镜继承的事实状态。\n- `backgroundNpcPolicy`：场景默认/必须/禁止背景 NPC，以及本镜头实际采用的数量、分布和行为；NPC 不进入角色资产引用。\n- `productionPlan.references.maxImages` 必须随镜头时长生成：15 秒及以下为 9，30 秒为 30；这只是短剧编排预算，供应商实际声明的更小能力仍由运行时能力校验决定。\n\n不得新增重复章节，不得用占位词代替当前节点事实。",
        gates: "硬门禁只处理事实和可执行性：资产引用、人物/道具/空间是否真实、当前帧是否有可见状态、入口/出口状态、实际尾帧、时间轴、参考职责和连续性依据。\n\n软警告处理电影化质量：光源动机、焦段与机位意图、纵深机制、景深使用、轴线风险、节奏重复、高潮停顿、非人物镜头职责和空泛电影术语。经验比例、固定运镜次数和题材偏好不得直接阻断生成。",
        externalCodex: "仅在用户显式选择本 Skill，或明确要求“审阅/优化制作包、分镜、片段帧、首尾帧、视频提示词”时执行。输出应说明发现的问题、保留的事实和可直接替换的公开字段；不得把内部规则、模型理由、供应商参数、内部 ID 或思维链写入公开提示词。",
    },
} as const;

const SURFACE_SECTIONS: Record<DramaDirectorSurface, string[]> = {
    package: ["common", "cinematography", "performance", "package", "gates"],
    "static-frame": ["common", "cinematography", "performance", "staticFrame", "gates"],
    video: ["common", "cinematography", "performance", "video", "gates"],
    "external-codex": ["common", "cinematography", "performance", "package", "staticFrame", "video", "gates", "externalCodex"],
};

export function resolveDramaDirectorInstructions(surface: DramaDirectorSurface) {
    return SURFACE_SECTIONS[surface]
        .map((section) => DRAMA_VIDEO_DIRECTOR_SKILL.sections[section as keyof typeof DRAMA_VIDEO_DIRECTOR_SKILL.sections])
        .filter(Boolean)
        .join("\n\n");
}

export function dramaDirectorSourceManifest() {
    return { id: DRAMA_VIDEO_DIRECTOR_SKILL.id, sourceVersion: DRAMA_VIDEO_DIRECTOR_SKILL.sourceVersion, sourceContentHash: DRAMA_VIDEO_DIRECTOR_SKILL.sourceContentHash } as const;
}
