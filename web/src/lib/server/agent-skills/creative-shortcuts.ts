import { SEEDANCE_25_DIRECTOR_SKILL } from "./seedance-25";
import { DRAMA_ASSET_IMAGE_SKILL } from "@/lib/drama-image-skill";

export { SEEDANCE_25_DIRECTOR_SKILL } from "./seedance-25";

export const CHARACTER_DESIGN_SKILL = {
    id: "character-design",
    name: "角色设定",
    description: "建立可持续复用的角色外观、服装、表情和多视图设定。",
    enabled: true,
    workspaces: ["image", "canvas", "drama"],
    action: "generate",
    requiresReference: false,
    defaultConfig: { quality: "high", count: 4 },
    keywords: ["角色设定", "角色设计", "人物设定", "角色图", "人物立绘", "多视图", "角色表"],
    instructions: `以角色设定工作流执行。先锁定身份、年龄、体态、脸部特征、发型轮廓、服装结构、材质、配色、关键道具和情绪范围，再生成正面、侧面、背面及表情/动作参考。每张图先明确用途和参考图控制范围；身份、比例、服装结构、发型轮廓、关键道具和色彩锚点必须跨视图保持一致。角色板只承载当前设定事实，不把安装说明、供应商参数或说明文字画进图片。后续修改使用 change/preserve/constraints，只改一个已定位变量，明确保留脸部、服装、姿态、构图和光线中不变的部分。所有成功结果都保留为独立候选，不因只需要一张主图而丢弃其他结果；不要随意改变种族、年龄、服装或脸部特征，也不要把多个视图生成成不同的人。`,
} as const;

export const IMAGE_MOTION_SKILL = {
    id: "image-motion",
    name: "图片动效",
    description: "把静态画面转成主体稳定、动作自然的短视频。",
    sourceUrl: "https://github.com/ArcReel/ArcReel",
    sourceVersion: "1.0.0",
    license: "MIT",
    enabled: true,
    workspaces: ["video"],
    action: "edit",
    requiresReference: true,
    defaultConfig: { videoSeconds: 5, vquality: "480" },
    keywords: ["图片动效", "图生视频", "图片转视频", "动效", "动画", "镜头推进", "首帧"],
    instructions: `以图生视频工作流执行。必须使用用户提供的参考图作为主体和首帧，并先说明每张参考图只控制身份、场景、构图或道具中的哪一项。提示词按静态锚点、可见起点、触发、主体动作、次级反应、一个主运镜、声音意图和可验证终点组织；只规划一个主要变化，保持人物、商品、场景、构图、色彩和文字位置稳定。动作前先核对场景中的座位、支撑面、通道、门窗和遮挡；人物位置、姿势、手脚接触、人与物距离及多人左右/前后关系必须符合现实，未声明人物不得入画。时长、比例和清晰度遵循用户与服务端配置，不在 Skill 中猜测供应商字段。避免新增人物、改变主体身份、重绘商品 Logo、过度运动、闪烁、瞬移、变形和无叙事理由的场景切换。失败重试只修改一个已定位变量，并保留已经通过验收的参考图与状态。`,
} as const;

export const DRAMA_CONTINUOUS_FRAME_RULES = `

连续动作帧规则：每个镜头先拆成“建立场景 → 镜头/主体推进 → 关键动作 → 结果或反应/转场”的可见节拍，再为每个节拍生成一张冻结画面。静态帧不是无动作的氛围图：每帧必须呈现该节拍已经发生的姿态、表情/视线、手部或道具位置、环境状态中的至少一项，并且与上一帧形成肉眼可辨的状态差异。动作提示词写“发生了什么”，画面提示词写“这一刻已经变成什么样”；不要让所有帧复用同一句场景描述后只添加“起始状态、动作展开、关键变化、结果状态”。

场景现实性与调度规则：在写任何帧或视频时间段前，先在内部完成“谁在何处、以什么姿势、借助什么结构、对谁或什么做什么”的场景推演。从已给场景资产提取可用的座椅/长凳、地面、通道、门窗、桌面、车辆舱位、遮挡物与前进方向；为每个实际出镜人物确定同一参照系下的位置、朝向、视线、手脚接触或受力对象和与其他出镜人物的相对关系。坐姿必须落在画面中明确可见的座位、长凳、床沿或地面等支撑面上，不能让人物蹲坐、跪坐、悬空或占据不合常理的空间；例如封闭马车中惊醒的人应坐在一侧长凳或明确座位，中央过道保持通行，并按车厢前进方向说明左右邻座人物或空位。若原文和资产未指定座位侧，必须选择与动作和机位相容的左侧或右侧座位，并将另一侧明确为已声明同伴或空位；禁止写“坐在车厢中央”“坐在座位旁”等模糊位置。多人同框必须写清人物彼此的左右/前后、朝向、视线与接触关系；只有一人时也必须写清其相对门窗、座位、通道或关键道具的位置。可依据场景类型补足实现动作所必需的普通空间结构，但不得虚构未在原文或资产中出现的人物、关系、剧情事件或道具；未声明的人应以空位、空座或画外存在处理，不得凭空入画。

若其他字段写出“坐在车厢中央”等与功能结构冲突的位置，以座位/通道的真实调度为准并修正；不得为突出主体牺牲支撑、接触和通行关系。

必要的连续性只锁定身份、服装、道具材质、空间轴线和光向；不得用“构图不变、主体稳定、情绪保持不变”等静态约束压住动作变化。时间段按实际动作节点和帧数连续分配，不预设固定秒数。`;

/** Shared public layout for generic video optimization and drama video prompts. */
export const SEEDANCE_VIDEO_PROMPT_LAYOUT = `
视频提示词公开布局（只输出提示词正文，不输出 Markdown 标题、解释或内部检查）：
动态意图：一句话写主体、主要事件和视觉目标。
全局设定：只写本镜头需要持续的环境、材质和主体不变量；同时交代行动实际依赖的空间结构、通道和支撑面，不要把完整资产档案复制进来。
起始可见状态：只写首帧已经可见的主体、道具、空间和动作状态；明确人物相对座位、门窗、通道或其他出镜人物的位置、朝向与接触关系。若有上一镜实际尾帧，只继承该真实边界状态。
触发：导致主要动作发生的可见事件；没有明确触发时省略，不要编造剧情。
主体动作与反应：按“准备 → 接触/受力 → 结果 → 恢复”写一个可观察动作链；人物情绪用眉眼、嘴角、下颌、视线、呼吸、手部或身体反应表达。
时间段动作：按 framePlan 的真实动作节点连续分段；每段独立成块，依次写“起点、动作与触发、可见衔接、终点”，时间范围连续、不重叠。每段都要保留合理的身体支撑、手脚接触、人与物距离和多人相对方位，不能为追求戏剧性让人物以不可能的姿势或位置完成动作。只在动作节点需要时增加帧，不按每一秒机械切碎；每段的终点必须成为下一段的起点。
单一主运镜：每镜只保留一个有动机的景别/机位/运镜，并说明它响应的动作或信息变化。
环境压力与视觉母题：至少写一个具体环境压力，以及一个声音或视觉锚点；不要用“电影感、震撼、氛围感”等空泛词代替。
视觉风格与光色：写可执行的色板、主光方向、材质反应和风格质感，不用空泛的“高级感”。
声音意图：对白写说话人、语言、语气和短句；另写环境声、动作音、音乐或静音、字幕策略。
结束画面：写最终可见的构图、姿态、道具位置、光色或声音状态。
连续性锁：只保留身份、数量、服装、道具归属、空间方向、光向和轴线等高代价约束；多人物明确同一镜头/场景参照系下的左右、前后和视线关系，未声明人物不得入画。
针对性约束：只列本镜头最可能且重做代价高的错误；不得添加内部 ID、URL、模型/API 参数或用户未提供的剧情事实。
规则：有参考素材时只继承其声明的属性；静态图生视频只描述运动和变化，不重复整张静态图；每镜只保留一个主要变化。镜头级字段只做摘要，具体动作和状态变化只写在“时间段动作”中，不在上方摘要重复。每个非空字段必须独立一行，禁止把所有字段压成一段逗号串。`;

export const DRAMA_PLANNING_SKILL = {
    id: "drama-planning",
    name: "短剧策划",
    description: "从主题到角色、场景和分镜，整理可继续生产的短剧方案。",
    sourceUrl: "https://github.com/ArcReel/ArcReel",
    sourceVersion: "1.0.0",
    license: "MIT",
    enabled: true,
    workspaces: ["image", "video", "drama"],
    action: "generate",
    requiresReference: false,
    defaultConfig: { count: 1, videoSeconds: 5 },
    keywords: ["短剧策划", "短剧", "剧本", "分镜", "剧集", "镜头", "故事板"],
    instructions: `以短剧生产工作流执行。按改编大纲、资产清单、剧本节拍、分镜、图片关键帧、视频提示词和审查的阶段顺序推进；每一阶段只修改自己的事实，不建立并行的文件或提示词真相。先核对主题、受众、冲突、角色、场景、道具、叙事节奏和集长，再按语速与动作节点核算时长。每个镜头必须有唯一戏剧职责、画面主体、对白/旁白、时长、景别、机位、声音、可见起点和终点，并通过稳定资产 ID 绑定角色、场景、道具和关键帧。对白和旁白可以不写进静态图片，但其可见后果必须进入对应帧；相邻帧必须有真实状态差异，禁止复制整镜提示词后只追加“起始/展开/结果”。${DRAMA_CONTINUOUS_FRAME_RULES}生成前展示准确的任务、参考和参数，付费生产等待用户明确确认；不要跳过结构分析，也不要在用户未明确要求时创建 Canvas 或短剧项目。`,
} as const;

export const SEEDANCE_STATIC_FRAME_PROMPT_SCHEME = `静态关键帧写法模板：
1. 静态关键帧：只写当前唯一可见瞬间。
2. 可见状态：主体、道具、环境在这一瞬间的状态。
3. 可见表演状态：眉眼、嘴角、下颌、视线、呼吸、手部或身体的可观察反应。
4. 景别：只写一个固定景别。面部、手部或道具要清晰时优先中远景、全身中景或更近景；ELS/极远景只能保留远景空间关系，不可与清晰脸部、手部或道具细节同时出现。
5. 机位与构图：平视/俯视/仰视、沿动作轴线、9:16安全区；前景必须是具体框景或遮挡物，不可空置。
6. 站位与视线：用镜头或场景前进方向作为同一参照系，写主体相对座位/通道/门窗/道具的位置、身体支撑和目光落点；多人同框写彼此左右或前后、朝向与关系，未声明人物不入画。
7. 三层空间：前景、中景、背景分别写清各自承载的元素，并让可用支撑面、通道、遮挡和空间纵深与人物动作相容。
8. 光色与风格：冷暖、主光、材质反应、风格气质。
9. 负面约束：只列本帧最可能且代价高的错误。`;

/** Shared line-oriented output contract for static storyboard/keyframe prompts. */
export const SEEDANCE_STATIC_FRAME_PROMPT_LAYOUT = `
静态帧提示词公开布局（只输出提示词正文，不输出 Markdown 标题、解释或内部检查）：
静态关键帧：只写当前唯一可见瞬间。
可见状态：主体、道具、环境在这一瞬间的状态。
可见表演状态：眉眼、嘴角、下颌、视线、呼吸、手部或身体的可观察反应。
景别：只写一个能承载所需细节的固定景别。
机位与构图：机位、主体位置、屏幕方向、9:16 安全区和具体前景框景。
站位与视线：用场景前进方向作为同一参照系，具体写明主体相对座位/长凳/床沿/通道/门窗/道具的位置、身体支撑、朝向和目光落点；多人同框写彼此左右或前后、朝向、视线和接触关系，未声明人物不入画。
三层空间：前景、中景、背景分别写明承载的元素，并让支撑面、通道、遮挡和空间纵深与人物动作相容。
光色与风格：色板、主光方向、材质反应和具体风格质感。
负面约束：只列本帧最可能且代价高的错误。
规则：每个非空字段必须独立成行；字段之间使用换行，不得用逗号或分号压成一段；没有事实的字段省略，不写“待补全”或空占位。`;

/** Shared static-frame checks used by drama prompts and the Seedance optimizer. */
export const SEEDANCE_STATIC_FRAME_RULES = `静态关键帧专用规则：这是单张冻结画面，不是完整视频提示词；不要添加时长、分时段、运镜过程、剪辑、对白或声音指令。输出前在内部完成一致性检查，但不得输出评估过程、思维链或检查清单。检查景别与细节是否相容：ELS/极远景只能承载远景空间关系，若同时要求清晰面部、手部或道具细节，必须消除冲突并改用能承载这些细节的远景全身或中远景，不得同时保留互相矛盾的景别。检查多手、多肢体、多主体的数量、来源、位置和连接关系；用户明确需要的手或肢体必须具体化，禁止用与目标冲突的笼统否定覆盖它们。先检查场景现实性：人物姿势必须有可见且合理的支撑/接触结构，动作路径不能穿过座椅、墙体、车厢隔断或其他人物；多人必须以同一参照系写清左右/前后、朝向、视线和接触关系。封闭马车、船舱、车内、餐桌、病床等有明确功能结构的场景，优先让人物使用对应座位、长凳、床沿、通道或桌面，不能为了突出主体把人摆在不合场景常理的位置；没有原文或资产依据时不得添加陪同者。凡是“上一帧/上一镜/严格连续”的要求，必须有对应的真实参考图或结构化入口状态；没有证据时改写为当前镜头可执行的独立构图约束，不得假定未绑定素材。每张 @图片、@视频、@音频只承担一个明确用途，编号必须与本次请求的 references 顺序一致；禁止凭标题、内部 ID 或文本相似度猜测用途。引用节点与画面正文分层：有 referenceManifest 时保留每项引用用途，不自行生成不存在的 @编号；最终供应商提示词中的 @图片节点必须由服务端按实际 references 数组顺序追加，编辑态 imagePrompt 未内嵌节点不代表引用丢失。把 Ras、Ref、内部编号等模型不可理解的内部词改为语义化的画面约束，除非它们是用户要求出现的可见内容。压缩重复的身份、服装、材质和禁止项，按主体与可见状态、场景、构图、光线、风格、连续性、负面约束排序；保留用户事实、数量、比例、参考职责和画幅，不新增剧情事实。

若功能性空间未给出座位侧，必须按场景前进方向选择左侧或右侧的明确座位，并把另一侧写成已声明同伴或空位；禁止用“坐在车厢中央”“坐在座位旁”等模糊位置代替调度。

${SEEDANCE_STATIC_FRAME_PROMPT_LAYOUT}
${SEEDANCE_STATIC_FRAME_PROMPT_SCHEME}`;

/**
 * Vetted through the GitHub Skill import contract. This is the mandatory
 * default for drama; the server-side continuity policy still runs separately.
 */
export const SEEDANCE_DIRECTOR_SKILL = {
    id: "seedance-director",
    name: "Seedance 导演",
    description: "为 Seedance 多镜头连续视频规划镜头、参考角色、续接和单变量返修。",
    plannerSummary: "为短剧镜头明确参考角色、连续性锚点、首尾帧与返修范围。",
    sourceUrl: "https://github.com/LeoYeAI/seedance-skills/blob/797e16efaa3c5ac01c0e391d0b8466a87cc5aadc/SKILL.md",
    sourceRepository: "LeoYeAI/seedance-skills",
    sourcePath: "SKILL.md",
    sourceVersion: "797e16efaa3c5ac01c0e391d0b8466a87cc5aadc",
    sourceCommit: "797e16efaa3c5ac01c0e391d0b8466a87cc5aadc",
    sourceContentHash: "8cbc9b6d27460f5ef3ff8313ecf506650c006be4979274ec63aea0aade9e3d25",
    license: "MIT",
    enabled: true,
    workspaces: ["drama"],
    action: "generate",
    requiresReference: false,
    defaultConfig: { videoSeconds: 5 },
    keywords: ["Seedance", "短剧导演", "连续性", "首尾帧", "镜头续接", "返修"],
    instructions: `按 Seedance 2.0 多模态短剧工作流执行。把长故事视为有入口、出口和连续性边界的镜头序列，而不是互不相关的提示词；先锁定镜头职责、时长、入口状态、出口状态、屏幕方向、轴线和每张参考图的唯一用途。每个镜头先从场景资产推演实际可用的座位、长凳、地面、通道、门窗、隔断和遮挡，再给每个实际出镜角色确定同一参照系下的位置、朝向、视线、支撑/接触对象与相对关系；坐姿必须落在可见座位或其他合理支撑面，封闭车厢内惊醒的人应坐在明确一侧长凳或座位而不是中央过道。多人必须写明彼此左右/前后和视线关系；没有原文或资产依据的人物不得补入画面。项目资产表中的角色名是正式业务事实，不得因为与 reference、ref 等英文缩写相似而改名、删除或当作内部占位符；已登记但本集/本镜不出镜的角色仍须保留在资产档案，并明确不得进入本集参考图请求。当前镜头只把实际出镜角色写入 characterCodes 和 referenceManifest；供应商提示词表达不出镜角色时，同时写角色名的不出镜约束和可观察画面限制，不得只写含义不清的“无可辨识的角色名”。参考图按角色、场景、道具、构图、首帧、尾帧或关键帧分工，@图片/@视频/@音频只表达用途，实际编号、顺序和 URL 由服务端绑定，提示词不得重复伪造参考清单。视频提示词按时间顺序写静态锚点、起始动作、触发、主体动作、次级反应、一个主运镜、环境压力、声音意图和明确结束画面；人物情绪必须转成起始/中段/结束可观察的眉眼、嘴角、下颌、视线、呼吸、手部或身体变化，禁止只写“电影感”“表情自然”“情绪丰富”等抽象词。只有上一镜当前视频版本且已人工验收的实际尾帧可以承担下一镜 first_frame；不得复制组内首镜起始状态，也不得引用旧分镜、旧任务或失效素材。用户明确的尺寸、比例、质量、时长和参考模式优先，不能擅自改选；每次返修只改变一个已定位变量，并保留已验收状态与引用。`,
} as const;

export const DEFAULT_CREATIVE_SHORTCUT_SKILLS = [CHARACTER_DESIGN_SKILL, IMAGE_MOTION_SKILL, DRAMA_PLANNING_SKILL, SEEDANCE_DIRECTOR_SKILL, SEEDANCE_25_DIRECTOR_SKILL, DRAMA_ASSET_IMAGE_SKILL] as const;
